package com.estofados.ecosidos.repository;

import com.estofados.ecosidos.domain.RawMaterialOrderLine;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RawMaterialOrderLineRepository extends JpaRepository<RawMaterialOrderLine, Long> {
}
