services:
  postgres:
    image: 'postgres:18.1-bookworm'
    environment:
      POSTGRES_DB: ${DB_NAME:-estofadosecosidos}
      POSTGRES_USER: ${DB_USERNAME:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
    ports:
      - '${DB_PORT:-5433}:5432'
